# HSU Process Manager - Go Implementation

Rust implementation of the HSU process management.

## Quick Start

```bash
# Build all crates
cargo build --workspace

# Run process manager
cd bins/hsu-process-manager
cargo run -- --config config-minimal.yaml --debug

# Run tests
cargo test --workspace
```

